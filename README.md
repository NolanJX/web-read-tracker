# Web Read Tracker

## Requirements

- Firefox

## Installation

### Firefox Local Installation

1. Set `xpinstall.signatures.required` to `false` in `about:config`
2. Run `pnpm zip:firefox`
3. `about:addons` → `Install Add-on From File...`
